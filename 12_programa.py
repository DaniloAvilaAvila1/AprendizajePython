#Manejo de paquetes
#PIP
#Podemos hacer operaciones mas eficientes y mas complejas de numeros.
import numpy #Con pip se instalo numpy
print(numpy.version.version)
numpy_array=numpy.array([35,24,62,52,30,30,17])
print(type(numpy_array))
print(numpy_array*2)

import pandas #Con pip se instalo pandas
#Hemos instalado librerias que mas adelante utilizaremos con la ayuda de pip.
#Se instalo la libreria requests
#Para utilizar un fichero siempre se hace un import.